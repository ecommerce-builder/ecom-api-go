Generate the init.sql file by running the following command inside the data directory.

```
$ $REPO_DIR/ecom-schemas/scripts/generate-docker-schema-init > init.sql
```

where `$REPO_DIR` is the home of your repositories.
